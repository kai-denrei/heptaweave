---
role: design
owner: claude-on-kainode
status: active
last-updated: 2026-09-03
---

# Design — heptaweave

## Scope
Visual language, micro-interactions, accessibility constraints, copy-free UX.

## Decisions
| Date | Decision | Rationale | Linked roles |
|---|---|---|---|
| 2026-05-20 | No letters, no Arabic numerals visible during play. Only ⧖, ∞, brush marks, dots, ring. | Spec. Heart of the project's identity. | [[pm]] |
| 2026-05-20 | Cream paper `#f6f1e7`, paper `#fbf7ee`, ink `#161310`. Paper-grain SVG noise behind. | Reuse heptacipher's palette. | |
| 2026-05-20 | Timer ring color: teal `#79ead9` → orange → red `#a83a2b` as time depletes. Linear-RGB interpolation. | Spec. Color cue is the only urgency signal (no countdown digits). | [[dev]] |
| 2026-05-20 | Choice circles ~80–100 px diameter at 7-up tier. Touch target ≥44 px. | Apple HIG; tier-7 fits in portrait. | [[pm]] |
| 2026-05-20 | Game-over screen is centered binary score + a single dot (green or red). Tap anywhere to return. | Spec; ritual of "the run is the artifact". | [[pm]] |
| 2026-05-20 | Landing screen: a centered ⧖ above an ∞ (or side-by-side), both tappable. No hint copy. | Spec: symbol-only. The icons are universal enough. | [[pm]] |
| 2026-05-20 | Choice layout = per-count organic quincunx slot presets (LAYOUT_PRESETS in main.js), not even-angle orbital. Matches the user's reference mockup. | Even-angle distribution felt mechanical. The mockup's varied positions (4-corner + bottom-center for 5 choices) gives the design "breath." | [[arch]] |
| 2026-05-20 | Cistercian dominates the center at `min(60vw, 340px)`. Choice tiles nestle close — slight overlap with the Cistercian's bounding box is intentional. | "Almost as if part of the same structure." Actual ink doesn't collide because the canonical V2 glyph leaves significant halo air around the rendered ensō. | [[arch]] |
| 2026-05-20 | Binary score row reads left-to-right, LSB first. Each bit is a brush mark: blob (`●`) for 1, hyphen (`–`) for 0. Always 8 marks. | Reading direction matches "the row fills from the left" intuition. Position 1 = bit 0 = first to light up. | [[dev]] |
| 2026-05-20 | Choice-glyph visual tune locked: gap 82°/46°, bulge 0.45/0.44/0.38, mark spread 0.8, dot 0.33 / dash 0.03, halo 0.85, wobble 0.5, vbPadFrac 0 (glyph fills the tile, no halo air margin). | User-dialed via the slider tuner, confirmed "satisfying." | [[dev]] |
| 2026-05-21 | theme_color = cream paper `#f6f1e7` (was ink `#161310`). manifest.webmanifest + index.html meta in sync. | The app's *background* is cream paper; theme_color should match that, not the foreground ink. iOS standalone status bar + Android chrome tint will now read as continuous paper. | [[dev]] |
| 2026-05-21 | All motion gated by `prefers-reduced-motion: reduce`: choice glow/shake animation neutralized; Cistercian opacity+blur transition becomes instantaneous; tile press-bounce removed; new-bit score pop disabled; mode-btn hover scale removed; toast + install affordance transitions zeroed. Discrete state changes still work (visibility, color). | OS accessibility setting MUST be honored — vestibular sensitivity is real. Game still plays; only the kinetic flourishes pause. | [[dev]] |
| 2026-05-21 | Meta-UI text policy: the "no Latin / no Arabic numerals during play" rule applies to the play surface. Update toast + install affordance appear ONLY on landing or game-over — meta-UI surfaces — and use universal symbols (↻ ⤓ ⊘ ✕) rather than text labels, to stay consistent with the project's symbol-only ethos. No words are introduced. | Symbols carry the same intent as text at a fraction of the noise. ↻ = "refresh," ⤓ = "save/install/download," ⊘ = "no signal," ✕ = "close." All near-universal. | [[pm]] |
| 2026-05-21 | Install affordance is symbol-only (⤓), 56px round, sits on the game-over screen below the score trinity. Cream-paper toast for SW updates, ↻ glyph + small dismiss ✕, 999px pill, sits above safe-area bottom. | Meta-UI must read as the same physical artifact as the rest of the app — same paper, same ink, same restraint. | [[dev]] |
| 2026-09-03 | **Ink theme: the dissolve is the timer.** ⧖ → run clock (glyph fades to the floor exactly when the run would end; stage light dims across the run; wrong = drain pulse; no ring). ∞ → reveal clock (glyph gone at revealMs; stays-tiers drift and re-ink). | User decision: "both, different modes." The glyph you are decoding is literally evaporating. | [[arch]] |
| 2026-09-03 | **Nothing fades until the whole glyph is on the page.** Trace (parallel brushes, all segments finish together) → hold in still water → ramp current + dissipation in. Sequential trace kept as a test-mode option. | User's concern: "by the time the last piece is drawn the first will have faded." Frozen water during paint makes both trace modes safe. | [[dev]] |
| 2026-09-03 | **Choices overlaid, feedback in the fluid (option C).** Logograms stay crisp SVG in the palette core colour with a CSS glow; the tapped glyph is splatted into the dye (core = correct bloom, ember = wrong splash + drain); siblings recede. Alternatives A/B recorded in `docs/devlog.md`. | Tap targets must stay legible; the fluid becomes the reaction channel. "We might have to try different implementations here." | [[arch]] |
| 2026-09-03 | **The Cistercian is drawn as one unbroken trait growing outward from the centre of the stem** — stem and all four digit shapes written simultaneously, no per-segment tapers, no endpoint ink puddles, no accents. The visual model is ink spreading from a source through a connected figure, not a pen writing strokes in sequence. | Operator's resolution of to-do 1. Removes the three accent sources inventoried below in one move, and replaces "when does each stroke start" with a single question: how fast does the front travel. | [[pm]] [[dev]] |
| 2026-09-10 | **◎ emblem**: the landing's fourth door, 72 px, top right, off the ⧖/∞ column and above the + pair; it is the numeral 12 in the new system (ring thin→thick, tip bar, foot bar) so it teaches the glyph before you tap it. | Operator: "medium top right as a simplified glyph of itself". 12 was chosen over 0 (bare ring reads as the heptaweave) and over busier numbers. | [[pm]] |
| 2026-09-10 | **Ring-stave rev 3: the enso's two ends carry the reading order.** Opening at the bottom (~29°); the brush starts thin at the bottom right and swells to thick at the bottom left; units by the thin end, highest place by the thick end; the growth front runs thin → thick so the units appear first. | Operator's call; the ends double as an unambiguous "start here" that needs no label. | [[pm]] [[dev]] |
| 2026-09-10 | **Ring-stave rev 2: the ring is the stave, no stems.** A digit spans an arc of 0.42 R between its tip and foot points and reaches 0.6 of that outward; 1/2 are radial bars, 5 a triangle closed by the ring, 6 a bar parallel to the ring, 9 a box on it. Eight slots, units upper-right, counter-clockwise for higher places; adjacent slots mirror tip/foot. | Operator dropped the stems on seeing the page; the tangential/radial reading makes the ring literally the stave. | [[pm]] [[dev]] |
| 2026-09-10 | **The logogram counter is diegetic**: a lobe's five lines are permanent; counting adds a drop on a line (fast, with a ripple where it lands) or lets a drop go (it fades over the beat, no ripple, the line shows through). 9→10 is one drop leaving the units and one landing on the tens, same beat. | Operator's spec, paraphrased and confirmed before building; the "line need not dissolve" answer made lines permanent and the model simpler. | [[dev]] [[pm]] |
| 2026-09-10 | **Black ink on rippling pale water is the look.** The result screen keeps its three score glyphs but in that ink, each hovering on its own slow breath; the clean/error dot stays crisp. The ring-stave numeral is drawable behind a lab switch: it grows from the bottom of the ring both ways, stems sprouting as the front passes. | Operator's call after living with the lab switches for one session. | [[pm]] |
| 2026-09-10 | **Inverted stage: black ink on a clear, pale ground; optional rippling water surface** (refraction, lit crests, touch rings, ambient pokes). Lives behind an invisible ⚙ top-right as lab switches. | Operator: "reverted. Black ink, clear background"; "try the black ink in a water surface like perhaps this one" (the koi pond). First frames read as ink on wet paper — promising, to be tuned. | [[dev]] |
| 2026-09-10 | **The logogram counter borrows the Cistercian counter's motion**: digits grow in fast and their old ink drifts off and dissolves on the same 0.9 s clock; the ring is pinned and never redrawn (it sweeps round once on entry). An invisible ⧉ in the top-left corner copies the deep link; it flashes once on success and is otherwise never seen. | Operator: "I like the size and disappearing motions of the Cistercian much better … except that the main circle does not get redrawn"; "a hidden ⧉ … does not show up, but if clicked it copies the url". | [[dev]] |
| 2026-09-09 | **Two + signs flank the landing**, left = Cistercian counter, right = logogram counter, both 84 px, level with the middle. Cistercian glyph at 0.7 of the short side, traced from the stem in 320 ms, a ghost by the next second. | Operator's layout ("another + on the left"). Left/right pairs the two glyph systems the game uses as prompt and choice. | [[pm]] |
| 2026-09-09 | **The count figure breathes** (2 % swell over 11 s, slow two-axis drift, drift 0.15×) and its brightness never steps: keep-alive ink arrives as a continuous trickle. | Operator: "the ring should also breathe a little, slow drift … it must all feel more organic." | [[dev]] |
| 2026-09-09 | **A count tick is a transition, not a swap**: over the full second the leaving digit's ink dissolves and the arriving digit's ink grows along the ring from one end of its lobe to the other (or dims in, `countReveal` 0). Marks both digits share stay put. | Operator: "smooth that animation with our ink tools, instead of 1 second click-click-click … it grows over 1 second … or dims". Same growth-front language as the Cistercian prompt. | [[dev]] |
| 2026-09-09 | **+ on the landing's right edge**, level with the middle, outside the ⧖/∞ column (84 px, the column stays 96 px). Count screen shows nothing but the ink; no score row, no chrome. The numeral holds nearly still (fade 0.996/step, drift 0.1×) and is drained in the last 300 ms so the change reads as a beat, not a fade. | Operator: "hourglass up top, mugen down, now a plus on the right side"; "ink effect but quite stable". | [[pm]] [[dev]] |
| 2026-09-09 | **Growth front runs at constant speed; width is constant; `taper` thins only free terminals; wobble is a long, low tremor that fades to zero at the root.** Prompt 0.4 of the short side (was 0.5). Choices sit in the deeper pair of free bands (above/below on a phone, left/right in landscape), gridded, all the same size, the far band taking the odd tile. | The five-trait read came from per-segment lens tapers plus bloom. Constant speed keeps the "ink spreading from a source" metaphor (short digits finish first). Tiles in the orbit were ~95 px on a phone with the top and bottom of the screen empty. | [[pm]] [[dev]] |
| 2026-09-03 | **Admin panel** (`#admin`, legacy `#test`, or the 1 s corner hold): one tab per parameter group (fluid / dissolve / paint / feedback / stage / rules) with the preset row pinned above them, sheet capped at 62vh, and `body.admin-open` lifts the play stage (`translateY(-26%) scale(.86)`) so the glyph being tuned stays visible above the sheet. Selected tab persists. | 42 sliders in one scroll is unusable on a phone; and a tuning surface that hides the thing you are tuning is worse than no surface. | [[dev]] |
| 2026-09-03 | **Test mode is a text-allowed dev surface** (`ink.html#test` or 1 s corner hold): grouped sliders for every param, local preset slots, shipped presets (`src/test/presets.js`), copy-link share (`#p=`), reset. | The no-Latin rule protects the play surface; a deliberate gesture / URL is the boundary. Same workflow that worked for the logogram tune, now phone-shareable. | [[pm]] |

## Dead Ends
<!-- APPEND ONLY. Never delete. -->
| Date | What was tried | Why it failed / was rejected |
|---|---|---|
| 2026-05-20 | First-pass logograms with all-outward lobes and a top-positioned gap, justified as "lobes-only renderer needs to stay readable at small size." | User feedback: "you got the basic design of the lobes wrong." Canonical heptacipher lobes alternate out/in/out/in starting from the bottom-left notch. The "small-size adaptation" was a solution to a wrong problem (the actual fix was larger tiles + canonical balance, not a degenerate design). |
| 2026-05-20 | Second pass: opened the ensō gap wide (55°) with a tiny tail (15°) to make the gap clearly visible, and beefed up wet-drop + mark sizes for "legibility." | User reference (image of original heptapod-logograms render) showed a much more subtle gap and balanced marks. My over-corrected version felt heavy-handed. Reverted to canonical V2 gap (32° + jitter), tail (22° + jitter), wet-drop (0.13×r), and made the visible-size tuning a separate parameter layer (HEPTAWEAVE_CHOICE_TUNE). |
| 2026-05-20 | Choice tiles laid out on an ellipse, evenly spaced. | User reference mockup shows an organic quincunx (4 corners + bottom-center for 5 choices), with varied distances from the Cistercian. Replaced even-angle with explicit slot presets per count. |

## Lessons
- **The user's reference images are precise about visual intent — copy the proportions, not the abstract design.** — from the ensō re-balance, 2026-05-20. When the user provides a reference image, treat its proportions (gap size, ink density, lobe amplitude, layout asymmetry) as the spec, not as inspiration. The original heptapod-logograms render is the canonical reference for this project.
- **"Organic" layout is achieved by varied positions, not by jitter on a regular grid.** — from the orbital-to-quincunx switch. Even-angle distribution with random offsets still reads as "geometric with noise." True organic feel comes from intentionally non-uniform anchor positions (the mockup's slot pattern), with only TINY jitter on top.

## Open Questions
- [x] ~~**Growth-front timing.** With a constant-speed front, long digit shapes finish later than short ones — true simultaneity, staggered completion, reads as ink physically spreading. Normalising so everything completes together (what the current parallel painter does) reads as "all done at once" and loses the source metaphor. Proposal: constant speed, since the operator's framing is a trait *growing* from the stem. Either way the ink theme's freeze still holds: nothing dissipates until the front has finished.~~ RESOLVED 2026-09-09: constant speed, built in the ink painter.  — owner: claude-on-kainode — since: 2026-09-03
- [ ] **How "one trait" reads in the paper theme.** Paper has no draw animation — it renders the glyph complete, then fades it. So "one trait" there means only the static half: one continuous brush path per connected figure, no accents. The growth is ink-theme-only. Check that a paper glyph drawn as one continuous path still reads as Cistercian without the puddles that used to weight its corners.  — owner: claude-on-kainode — since: 2026-09-03
- [x] ~~**One-trait Cistercian (TO-DO 1).**~~ RESOLVED 2026-09-03 — see Decisions. The accents this removes, kept here as the inventory to verify against: (1) `cistercianInk.js` drops an `irregularBlob` ink puddle at every non-stave segment endpoint (line ~141) — with polylines shredded into 2-point segments, a digit 9 gets three puddles inside one stroke; (2) both themes taper each segment independently (`lensProfile` in paper, the per-run lens in the ink painter), so every interior joint reads as a fresh stroke start; (3) per-segment perpendicular wobble means the joints do not even meet on a smooth curve. Design question to settle first: per-digit continuity, or one unbroken trait for the whole numeral (see [[pm]]). Note the puddles were deliberate in May ("thickens junctions so a 5 doesn't look thin at the corner") — removing them re-opens the thin-corner problem that they solved, so continuity has to carry that weight instead.  — owner: claude-on-kainode — since: 2026-09-03
- [ ] **Inverted mode (TO-DO 2, confirmed) — design questions still open:** at prompt scale the ensō's tuned proportions (locked 2026-05-20 for ~100 px tiles) will need re-checking at ~300 px; the Cistercian then becomes the choice tile, and it has never been drawn small; and in the ink theme a round prompt dissolves very differently from a stick figure — a closed ring under curl flow collapses into a blob rather than unspooling into filaments (visible in this session's `fluid-ring` test frame). The dissolve-as-timer read may not survive the switch unchanged.  — owner: claude-on-kainode — since: 2026-09-03
- [ ] Landing layout — vertical stack or horizontal pair? Default v1: vertical, ⧖ on top.  — owner: claude-on-kainode — since: 2026-05-20
- [ ] Should the binary row "pulse" briefly when a new bit is added? Default v1: yes, 250ms scale ramp on the new bit.  — owner: claude-on-kainode — since: 2026-05-20
- [ ] Wet-bleed filters on choice circles — full halo+crisp pipeline is expensive at 7-up. Default v1: simplify — crisp only on small choices, halo only on the big Cistercian. — owner: claude-on-kainode — since: 2026-05-20

## Assumptions
- A green dot vs a red dot is an unambiguous "clean vs error" signal in this minimal UI. — status: untested — since: 2026-05-20

## Dependencies
Blocked by:
Feeds into: [[dev]]

## Session Log
- 2026-09-09 (+ count mode) — Third symbol placed off-column on the right; count screen is ink only.
- 2026-09-09 (one trait / bands / diffusion) — Front timing resolved (constant speed). Band layout replaces the orbit in ink. Bloom defaults lowered and a diffusion knob added; the look is now the operator's to tune. Still open: how one-trait reads in paper.
- 2026-09-03 (to-do intents resolved) — One-trait Cistercian specified as a radial growth front from the stem's centre; accent inventory retained as the verification checklist. New questions: front timing (constant speed vs normalised completion) and what "one trait" means in the paper theme, which has no draw animation.
- 2026-09-03 (ink v1 + promotion) — Ink accepted as v1 and promoted to default; admin panel tabbed and the play stage lifts while it is open. Two to-dos captured with their design consequences: the one-trait Cistercian (accent inventory above; note it re-opens the thin-corner problem the puddles solved) and a large-round-glyph mode (ensō proportions were tuned at tile scale, and a ring dissolves into a blob rather than filaments).
- 2026-05-21 (PWA hardening) — theme_color flipped to cream paper. prefers-reduced-motion wrap added across all keyframes / transitions. Meta-UI surfaces (update toast, install affordance, offline page) introduced and styled in the cream/ink palette using symbol-only labels (↻ ⤓ ⊘ ✕).
- 2026-05-20 (tune session) — Layout decision locked (organic quincunx). Cistercian sized up to `min(60vw, 340px)` so it dominates. Score representation fixed to 8-bit LSB-left. Choice-glyph visual constants captured from the user's tune session. Two design lessons added (proportions-as-spec, organic-via-varied-positions).
- 2026-05-20 — Design role seeded.
